# Настройка Telegram уведомлений для заявок

## Самый простой способ получать заявки - через Telegram бота

### 🎉 Новая функция: Автоматическая подписка!

Теперь можно просто **отправить бота другу/коллеге**, и когда они нажмут `/start`, они автоматически начнут получать заявки! Не нужно вручную добавлять Chat ID в `.env`.

### Шаг 1: Создайте бота через @BotFather

1. **Откройте Telegram** и найдите бота **@BotFather**
2. Отправьте команду `/newbot` или нажмите кнопку "Create a new bot"
3. **Придумайте имя для бота** (например: "Витязь Заявки" или "Заявки с сайта")
   - Это имя будет видно в списке контактов
4. **Придумайте username для бота** (должен заканчиваться на `bot`)
   - Например: `vityaz_leads_bot` или `my_site_bot`
   - Username должен быть уникальным и содержать только латинские буквы, цифры и подчёркивания
5. **Скопируйте токен** который даст вам BotFather
   - Токен выглядит как: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
   - ⚠️ **Сохраните токен в безопасном месте!** Он нужен для настройки

**Дополнительно (опционально):**
- Можете настроить описание бота: `/setdescription` (выберите вашего бота)
- Можете добавить фото: `/setuserpic` (выберите вашего бота)

### Шаг 2: Узнайте свой Chat ID

Есть несколько способов:

**Способ 1: Через @userinfobot (самый простой)**
1. Найдите бота **@userinfobot** в Telegram
2. Отправьте ему любое сообщение (например `/start`)
3. Он пришлёт вам ваш **Chat ID** (выглядит как `123456789`)

**Способ 2: Через вашего бота**
1. Напишите вашему боту любое сообщение (например "Привет")
2. Откройте в браузере: `https://api.telegram.org/botВАШ_ТОКЕН/getUpdates`
   - Замените `ВАШ_ТОКЕН` на токен от BotFather
3. Найдите в ответе поле `"chat":{"id":123456789}` - это ваш Chat ID

**Способ 3: Через @getidsbot**
1. Найдите бота **@getidsbot** в Telegram
2. Отправьте `/start`
3. Он покажет ваш Chat ID

### Шаг 3: Настройка получателей

**Вариант 1: Отправка нескольким людям (каждому отдельное сообщение)**

Если вы хотите, чтобы заявки приходили нескольким людям, укажите их Chat ID через запятую:

1. Узнайте Chat ID каждого получателя (см. Шаг 2)
2. В файле `.env` укажите все Chat ID через запятую:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=123456789,987654321,555666777
```

**Вариант 2: Групповой чат (все видят одно сообщение)**

Если вы хотите, чтобы все заявки приходили в группу, где все участники видят сообщения:

1. Создайте группу в Telegram
2. Добавьте вашего бота в группу (найдите бота по username и добавьте)
3. Отправьте любое сообщение в группу (например "Привет")
4. Узнайте Chat ID группы:
   - Откройте: `https://api.telegram.org/botВАШ_ТОКЕН/getUpdates`
   - Найдите `"chat":{"id":-123456789}` (Chat ID группы начинается с минуса!)
5. В файле `.env` укажите Chat ID группы:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=-123456789
```

**Вариант 3: Канал (для публичных уведомлений)**

1. Создайте канал в Telegram
2. Добавьте бота как администратора канала
3. Узнайте Chat ID канала (обычно начинается с `-100`)
4. Укажите в `.env`:
```env
TELEGRAM_CHAT_ID=-1001234567890
```

### Шаг 4: Настройте в .env файле

1. Откройте файл `.env` (создайте его из `env.example.txt` если его нет)
2. Добавьте или обновите эти строки:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id
```

**Пример для одного получателя:**
```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

**Пример для нескольких получателей:**
```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789,987654321,555666777
```

3. Сохраните файл
4. Перезапустите сервер (если он запущен)

### Шаг 5: Проверьте работу

1. Отправьте тестовую заявку через форму на сайте
2. Проверьте Telegram - должно прийти сообщение с заявкой
3. Если не пришло, проверьте:
   - Правильно ли скопирован токен (без пробелов)
   - Правильно ли указан Chat ID
   - Запущен ли сервер
   - Проверьте логи сервера на ошибки

### Готово! 🎉

Теперь все новые заявки будут автоматически приходить вам в Telegram в формате:

---

## 🚀 Как добавить других получателей (новый способ!)

### Просто отправьте бота другу/коллеге:

1. **Найдите вашего бота** в Telegram (по username, который вы указали при создании)
2. **Отправьте бота** другу/коллеге (через "Переслать сообщение" или просто username бота)
3. **Попросите их** написать боту `/start`
4. **Готово!** Теперь они тоже будут получать все заявки автоматически

**Команды бота:**
- `/start` - подписаться на уведомления о заявках
- `/stop` - отписаться от уведомлений
- `/status` - проверить статус подписки

**Важно:** Бот автоматически обрабатывает команды. Не нужно перезапускать сервер или редактировать `.env` файл!

---

## 📋 Формат уведомлений

Все новые заявки будут автоматически приходить в Telegram в формате:
```
📨 Новая заявка
Имя: Иван Иванов
Телефон: +7 (914) 578-36-55
Сообщение: Хочу записаться на тренировку
```

---

## Полезные команды BotFather

Если нужно изменить настройки бота позже:

- `/mybots` - список ваших ботов и управление ими
- `/token` - получить токен бота заново (если потеряли)
- `/setname` - изменить имя бота
- `/setdescription` - изменить описание бота
- `/setuserpic` - добавить/изменить фото бота
- `/deletebot` - удалить бота (если больше не нужен)

---

## Альтернативные способы получения заявок:

### 1. WhatsApp уведомления
- Настройте через CallMeBot (см. `WHATSAPP_SETUP.md`)
- Заявки будут приходить в WhatsApp

### 2. Email уведомления (опционально)
- Настройте SMTP в `.env` файле (см. `env.example.txt`)
- Заявки будут дублироваться на email

### 3. Комбинация способов
- Можно настроить Telegram + WhatsApp + Email одновременно
- Заявки будут дублироваться во все настроенные каналы

⚠️ **Важно:** Заявки **НЕ сохраняются** на сервере! Они отправляются только в мессенджеры/email. Обязательно настройте хотя бы один способ уведомления!

---

## Преимущества Telegram:
✅ Мгновенные уведомления на телефон  
✅ Не нужно настраивать почту  
✅ Работает бесплатно  
✅ Можно настроить за 2 минуты  
✅ Уведомления приходят даже если сайт не открыт

