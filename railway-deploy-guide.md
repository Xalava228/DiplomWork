# 🚀 Гайд по деплою на Railway

## Быстрый старт

### 1. Подготовка проекта

Убедитесь, что в проекте есть файлы:
- ✅ `index.html`
- ✅ `style.css`
- ✅ `script.js`
- ✅ `package.json` (уже создан)
- ✅ `server.js` (уже создан)
- ✅ Папка `assets/` с изображениями

### 2. Регистрация на Railway

1. Перейдите на https://railway.com/
2. Нажмите **"Start a New Project"**
3. Войдите через GitHub (рекомендуется)

### 3. Деплой проекта

#### Вариант А: Деплой из GitHub (рекомендуется)

1. **Создайте репозиторий на GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
   git push -u origin main
   ```

2. **В Railway:**
   - Нажмите **"Deploy from GitHub repo"**
   - Выберите ваш репозиторий
   - Railway автоматически определит Node.js проект
   - Деплой начнётся автоматически

#### Вариант Б: Деплой через Railway CLI

1. **Установите Railway CLI:**
   ```bash
   npm i -g @railway/cli
   ```

2. **Войдите:**
   ```bash
   railway login
   ```

3. **Деплой:**
   ```bash
   railway init
   railway up
   ```

### 4. Настройка домена (опционально)

1. В Railway Dashboard откройте ваш проект
2. Перейдите в **Settings** → **Domains**
3. Нажмите **"Generate Domain"** или добавьте свой домен
4. Railway автоматически выдаст HTTPS сертификат

### 5. Переменные окружения (если нужно)

Если понадобятся переменные окружения:
1. **Settings** → **Variables**
2. Добавьте нужные переменные (например, API ключи)

## 📝 Важные моменты

- Railway автоматически определит Node.js проект по `package.json`
- Порт определяется через переменную `PORT` (Railway устанавливает её автоматически)
- После каждого push в GitHub проект автоматически передеплоится
- Логи можно смотреть в разделе **Deployments** → **View Logs**

## 🔧 Структура проекта

```
diplom/
├── index.html
├── style.css
├── script.js
├── server.js          ← новый файл
├── package.json        ← новый файл
└── assets/
    └── ...
```

## ✅ Проверка

После деплоя:
1. Railway выдаст URL вида: `ваш-проект.up.railway.app`
2. Откройте его в браузере
3. Сайт должен работать!

## 💡 Полезные команды

```bash
# Посмотреть логи
railway logs

# Открыть проект в браузере
railway open

# Посмотреть статус
railway status
```

## 🆘 Если что-то не работает

1. Проверьте логи в Railway Dashboard
2. Убедитесь, что все файлы закоммичены в Git
3. Проверьте, что `package.json` и `server.js` на месте
4. Убедитесь, что порт в `server.js` использует `process.env.PORT`

---

**Готово!** Ваш сайт теперь доступен в интернете 🎉
