# Рекомендации по безопасности и готовности к продакшену

## ✅ Что уже реализовано:

### Безопасность:
1. **Helmet.js** - защита заголовков HTTP
2. **Rate Limiting** - ограничение запросов (логин, админ-операции, формы)
3. **CSRF защита** - токены для всех админ-операций
4. **Валидация загрузки файлов** - проверка расширений и MIME-типов
5. **Санитизация входных данных** - защита от XSS
6. **Валидация UUID** - проверка параметров запросов
7. **Защита от path traversal** - валидация URL
8. **Ограничение размера файлов** - максимум 5MB
9. **Ограничение размера JSON** - максимум 200KB
10. **Блокировка доступа к /data** - защита конфиденциальных данных

### SEO и мета-теги:
1. **Open Graph** - мета-теги для соцсетей
2. **Twitter Cards** - мета-теги для Twitter
3. **robots.txt** - инструкции для поисковых роботов
4. **sitemap.xml** - карта сайта для поисковиков

### Продакшен:
1. **Health check endpoint** - `/health` для мониторинга
2. **Условное логирование** - console.error только в development
3. **Улучшенный .gitignore** - защита чувствительных данных

## ⚠️ Что нужно сделать перед деплоем:

### 1. Настройка переменных окружения (.env):
```env
NODE_ENV=production
PORT=3000

# Обязательно измените эти значения!
SESSION_SECRET=случайная_длинная_строка_минимум_32_символа
ADMIN_PASSWORD=ваш_сильный_пароль_минимум_12_символов

# SMTP настройки (для отправки заявок)
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=ваш_email@yandex.ru
SMTP_PASS=пароль_приложения_из_яндекса
LEADS_EMAIL_TO=куда_отправлять_заявки@yandex.ru

# Опционально: Telegram уведомления
TELEGRAM_BOT_TOKEN=ваш_токен
TELEGRAM_CHAT_ID=ваш_chat_id
```

### 2. Обновите домены в файлах:
- `robots.txt` - замените `yourdomain.com` на ваш домен
- `sitemap.xml` - замените `yourdomain.com` на ваш домен
- `index.html` - обновите Open Graph мета-теги с вашим доменом

### 3. Проверьте пароли:
- **НЕ используйте** дефолтный пароль `ChangeMe123!`
- Используйте сильный пароль (минимум 12 символов, буквы, цифры, спецсимволы)
- Можно сгенерировать хеш пароля: `node -e "console.log(require('bcryptjs').hashSync('ваш_пароль', 10))"` и использовать `ADMIN_PASSWORD_HASH`

### 4. Настройте уведомления о заявках (ОБЯЗАТЕЛЬНО!):

⚠️ **КРИТИЧЕСКИ ВАЖНО:** Заявки **НЕ сохраняются** на сервере! Они отправляются только в мессенджеры/email. Обязательно настройте хотя бы один способ!

**Вариант 1: Telegram (РЕКОМЕНДУЕТСЯ - самый простой)**
- См. инструкцию в `TELEGRAM_SETUP.md`
- Настройка занимает 2 минуты
- Мгновенные уведомления на телефон

**Вариант 2: WhatsApp (тоже просто)**
- См. инструкцию в `WHATSAPP_SETUP.md`
- Настройка через CallMeBot за 2 минуты
- Мгновенные уведомления на телефон

**Вариант 3: Email (опционально)**
- Настройте SMTP в `.env` (см. `env.example.txt`)
- Более сложная настройка, требует пароль приложения

**Можно настроить несколько способов одновременно** - заявки будут дублироваться во все настроенные каналы.

### 5. Настройте HTTPS:
- В продакшене обязательно используйте HTTPS
- Убедитесь что `NODE_ENV=production` установлен
- Helmet автоматически включит `secure` для cookies

### 6. Резервное копирование:
- Настройте автоматическое резервное копирование папки `data/`
- Рекомендуется делать бэкапы ежедневно

### 7. Мониторинг:
- Настройте мониторинг endpoint `/health`
- Настройте алерты на ошибки сервера
- Рекомендуется использовать сервисы типа UptimeRobot, Pingdom

### 8. Лимиты и производительность:
- Проверьте лимиты rate limiting (сейчас: 10 логинов/15мин, 50 админ-операций/5мин, 20 заявок/5мин)
- При необходимости увеличьте лимиты для вашего трафика

### 9. Дополнительные рекомендации:
- Рассмотрите использование CDN для статических файлов
- Настройте кэширование для статических ресурсов
- Используйте reverse proxy (nginx) перед Node.js приложением
- Настройте логирование в файлы (winston, pino)
- Рассмотрите использование базы данных вместо JSON файлов при росте данных

## 🔒 Дополнительные меры безопасности (опционально):

1. **IP Whitelist для админки** - ограничьте доступ к `/admin` по IP
2. **2FA** - двухфакторная аутентификация для админки
3. **WAF** - Web Application Firewall (Cloudflare, AWS WAF)
4. **DDoS защита** - используйте Cloudflare или аналоги
5. **Регулярные обновления** - обновляйте зависимости (`npm audit`, `npm update`)

## 📝 Чеклист перед запуском:

- [ ] Создан `.env` файл с правильными значениями
- [ ] Изменён `SESSION_SECRET` на случайную строку
- [ ] Изменён `ADMIN_PASSWORD` на сильный пароль
- [ ] Обновлены домены в `robots.txt` и `sitemap.xml`
- [ ] Обновлены Open Graph мета-теги в `index.html`
- [ ] Настроены уведомления о заявках (ОБЯЗАТЕЛЬНО! Telegram или WhatsApp, или Email)
- [ ] Проверена работа админ-панели
- [ ] Проверена отправка заявок
- [ ] Настроен HTTPS
- [ ] Настроен мониторинг
- [ ] Настроено резервное копирование

## 🚀 Готово к деплою!

Ваш сайт защищён и готов к работе в продакшене. Все критические уязвимости устранены.


